# Nano is the default editor
export EDITOR="nano"

# Larger bash history (allow 32³ entries; default is 500)
export HISTSIZE=32768
export HISTFILESIZE=$HISTSIZE
export HISTCONTROL=ignoredups
# Make some commands not show up in history
export HISTIGNORE="ls:cd:cd -:pwd:exit:date:* --help:code .:zz *"
# And include the parameter for ZSH
export HISTORY_IGNORE="(ls|cd|cd -|pwd|exit|date|* --help|code .|zz *)"

export TORCHLIGHT_API_TOKEN="torch_b9zcLCT0p2YAkKAjGDHYjX1qzwfLvQjMbW0MscB7"

export NODE_PATH="/Users/jamessessford/.nvm/versions/node/v18.1.0/lib/node_modules"
